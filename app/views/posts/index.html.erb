
<h3> Posts </h3>	
<ul>
  <% @posts.each do |post| %>
    <li><%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>
<% if user_signed_in? %>
	<% if @user.email == "lizzie@gmail.com" %>
		<%= link_to "New Post", new_post_path, class: ['btn'] %>
	<% end %>
<% end %>