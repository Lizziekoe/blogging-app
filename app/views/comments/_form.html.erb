<div class="form">	
	<%= form_for [@post, @comment] do |f| %>
	 <div class="form-group">
	  <%= f.label :content %>
	  <%= f.text_field :content %>
	 </div>
	 <div class="form-group">
	  <%= f.label :name %>
	  <%= f.text_field :name %>
	 </div>
	  <p><%= f.submit %></p>
	<% end %>
</div>